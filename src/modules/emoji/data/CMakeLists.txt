if (DEFINED FCITX5_MODULE_EMOJI_DICT_FILES)
    set(DICT_FILES ${FCITX5_MODULE_EMOJI_DICT_FILES})
else()
    file(GLOB DICT_FILES *.dict)
endif()

install(FILES ${DICT_FILES} DESTINATION ${FCITX_INSTALL_PKGDATADIR}/emoji/data
        COMPONENT config)

